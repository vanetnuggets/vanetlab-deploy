version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./vanetlab-be:/app
      - ./vanetlab-ns3:/vlns3
    command: bash
    ports:
      - "5000:5000"
    env_file:
      - .env
